<Window x:Class="Sportics.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
        xmlns:vm="clr-namespace:Sportics.ViewModel"
        xmlns:helper="clr-namespace:Sportics.Helper"
        mc:Ignorable="d"
        Title="Sportics"
        MinHeight="500" MinWidth="800"
        Width="1250" Height="650"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <helper:ImageSourceConverter x:Key="ImageConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Верхняя панель -->
            <RowDefinition Height="*"/>
            <!-- Контент -->
            <RowDefinition Height="Auto"/>
            <!-- Нижняя панель -->
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Grid Grid.Row="0" Background="{DynamicResource MaterialDesignPaper}" Height="60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <!-- Лево: логотип -->
                <ColumnDefinition Width="Auto" />
                <!-- Центр: кнопки -->
                <ColumnDefinition Width="*" />
                <!-- Право: иконки -->
            </Grid.ColumnDefinitions>

            <!-- Левая часть: логотип и название -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="40,0" Grid.Column="0">
                <svg:SvgViewbox Source="/Resources/Images/dumbbell.svg" Height="40" Width="40" />
                <Label Content="SPORTICS" FontSize="24" VerticalAlignment="Center" Margin="10,0"/>
            </StackPanel>

            <!-- Центральная часть: кнопки -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1">
                <Button Content="Абонементы" FontSize="16" Margin="0,0,10,0" Width="150" Command="{Binding OpenMembershipsCommand}"/>
                <Button Content="Расписание" FontSize="16" Margin="10,0" Width="150" Command="{Binding OpenSchedulesCommand}"/>
                <Button Content="Тренеры" FontSize="16" Margin="10,0" Width="150" Command="{Binding OpenCoachesCommand}"/>
            </StackPanel>

            <!-- Правая часть: иконки -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Grid.Column="2">
                <ToggleButton Content="🌙"
                        IsChecked="{Binding IsDarkTheme, Mode=TwoWay}"
                        ToolTip="Тема"
                        Width="40" Height="40"
                        VerticalAlignment="Center"
                        Margin="10,0,10,0"/>

                <Border Background="{DynamicResource MaterialDesignPaper}" Margin="10">
                    <Border.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding OpenAccountCommand}" />
                    </Border.InputBindings>
                    <svg:SvgViewbox Source="Resources\Images\user.svg" Height="30" Width="30"/>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Основной контент -->
        <ScrollViewer Grid.Row="1" Margin="0">
            <StackPanel Background="{DynamicResource MaterialDesignBackground}">
                <Label Content="Предложения недели" FontSize="32" HorizontalAlignment="Center" Margin="0,20" Foreground="{DynamicResource MaterialDesignBody}"/>

                <ItemsControl ItemsSource="{Binding WeeklyOffers}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="250" Margin="30">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="2"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" Value="DodgerBlue"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Border Background="{DynamicResource MaterialDesignPaper}" BorderThickness="1">
                                    <StackPanel Margin="10">
                                        <Image Height="120" Stretch="Fill" Margin="5"
                                                Source="{Binding Photo, Converter={StaticResource ImageConverter}}"/>
                                        <TextBlock Text="{Binding ShortName}" FontSize="16" FontWeight="SemiBold" TextAlignment="Center" Margin="0,10" Foreground="{DynamicResource MaterialDesignBody}"/>
                                        <TextBlock Text="{Binding Category}" FontSize="16" FontWeight="SemiBold" TextAlignment="Center" Margin="0,10" Foreground="{DynamicResource MaterialDesignBody}"/>
                                        <TextBlock Text="{Binding Price, StringFormat=Цена: {0:C}}" FontSize="16" FontWeight="Bold" TextAlignment="Center" Margin="0,5" Foreground="{DynamicResource MaterialDesignBody}"/>
                                        <Button Content="Подробнее"
                                            Command="{Binding DataContext.DetailsCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}" 
                                            Width="150" Margin="0,10" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </ScrollViewer>

        <!-- Нижняя панель -->
        <Border Grid.Row="2" Background="{DynamicResource MaterialDesignPaper}" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,0">
                <svg:SvgViewbox Source="Resources\Images\Mail.svg" Height="20" Width="20" Margin="0,0,5,0"/>
                <Label Content="sportics@gmail.com" VerticalAlignment="Center"/>
                <svg:SvgViewbox Source="Resources\Images\Phone.svg" Height="20" Width="20" Margin="20,0,5,0"/>
                <Label Content="+375 (44) 573-04-02" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
