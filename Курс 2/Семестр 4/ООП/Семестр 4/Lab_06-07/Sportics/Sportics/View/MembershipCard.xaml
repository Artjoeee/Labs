<UserControl x:Class="Sportics.View.MembershipCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Sportics.View"
             xmlns:helper="clr-namespace:Sportics.Helper"
             Width="250" Height="371">

    <UserControl.Resources>
        <DropShadowEffect x:Key="CardShadow" ShadowDepth="5" BlurRadius="10" Opacity="0.4"/>
        <helper:ImageSourceConverter x:Key="ImageConverter"/>
    </UserControl.Resources>

    <Border x:Name="RootBorder"
            CornerRadius="15"
            Background="{DynamicResource MaterialDesignPaper}"
            Margin="10"
            Effect="{StaticResource CardShadow}">
        <Border.RenderTransform>
            <ScaleTransform x:Name="CardScale" ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>

        <Border.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="CardScale"
                                         Storyboard.TargetProperty="ScaleX"
                                         To="1.01" Duration="0:0:0.2"/>
                        <DoubleAnimation Storyboard.TargetName="CardScale"
                                         Storyboard.TargetProperty="ScaleY"
                                         To="1.01" Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="CardScale"
                                         Storyboard.TargetProperty="ScaleX"
                                         To="1" Duration="0:0:0.2"/>
                        <DoubleAnimation Storyboard.TargetName="CardScale"
                                         Storyboard.TargetProperty="ScaleY"
                                         To="1" Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <StackPanel Margin="15">
            <Border CornerRadius="10" ClipToBounds="True" Height="120">
                <Image Stretch="UniformToFill"
           Source="{Binding Photo, Converter={StaticResource ImageConverter}}"/>
            </Border>


            <TextBlock Text="{Binding ShortName}" FontSize="18" FontWeight="Bold"
                       TextAlignment="Center" Margin="0,5" Foreground="{DynamicResource MaterialDesignBody}"/>

            <TextBlock Text="{Binding Category}" FontSize="14" TextAlignment="Center"
                       Margin="0,5" Foreground="{DynamicResource MaterialDesignBody}"/>

            <TextBlock Text="{Binding Price, StringFormat='Цена: {0:C}'}" FontSize="16"
                       FontWeight="SemiBold" TextAlignment="Center" Margin="0,5"
                       Foreground="{DynamicResource MaterialDesignBody}"/>

            <Button Content="Подробнее" Width="120" Margin="0,10" HorizontalAlignment="Center"
                    Command="{Binding DataContext.DetailsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="{Binding}"/>

            <Button Content="Удалить"
            Command="{Binding DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
            CommandParameter="{Binding Membership, RelativeSource={RelativeSource AncestorType=UserControl}}"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="5" />
        </StackPanel>
    </Border>
</UserControl>
