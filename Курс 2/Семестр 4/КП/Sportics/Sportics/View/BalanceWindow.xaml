<Window x:Class="Sportics.View.BalanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:Sportics.ViewModel"
        xmlns:helper="clr-namespace:Sportics.Helper"
        xmlns:local="clr-namespace:Sportics.View"
        mc:Ignorable="d"
        MinHeight="350" MinWidth="400"
        Title="" Height="400" Width="500" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:BalanceViewModel/>
    </Window.DataContext>

    <Grid Background="{DynamicResource MaterialDesignPaper}" Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Пополнение баланса" FontSize="24" FontWeight="SemiBold" TextAlignment="Center"/>
            <TextBlock Text="Введите сумму:" Foreground="{DynamicResource MaterialDesignBody}" Margin="0,30,0,5" />
            <TextBox Text="{Binding Money, ValidatesOnDataErrors=True, NotifyOnValidationError=True, UpdateSourceTrigger=PropertyChanged}"
                Foreground="{DynamicResource MaterialDesignBody}" 
                Margin="0,0,0,10"
                materialDesign:HintAssist.Hint="Сумма"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Пополнить" Height="35" Margin="0,15,5,0" Command="{Binding AddBalanceCommand}" Width="180"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
