<Window x:Class="Sportics.View.AddScheduleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:Sportics.ViewModel"
        mc:Ignorable="d"
        Title="Добавить занятие" Height="500" Width="400" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:AddScheduleViewModel />
    </Window.DataContext>

    <Grid Background="WhiteSmoke" Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Добавить занятие" FontSize="24" FontWeight="SemiBold" TextAlignment="Center"/>

            <ComboBox materialDesign:HintAssist.Hint="Категория"
                      SelectedItem="{Binding Category, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                      ItemsSource="{Binding Categories}"
                      Margin="0,20,0,0"/>

            <DatePicker materialDesign:HintAssist.Hint="Дата"
                        SelectedDate="{Binding Date, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,20,0,0"/>

            <TextBox materialDesign:HintAssist.Hint="Время (чч:мм)"
                     Text="{Binding Time, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,20,0,0"/>

            <ComboBox materialDesign:HintAssist.Hint="Тренер"
                      SelectedItem="{Binding Coach, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                      ItemsSource="{Binding Coaches}"
                      Margin="0,20,0,0"/>

            <Button Content="Создать" Height="35" Margin="0,20,0,0" Command="{Binding AddScheduleCommand}" />
        </StackPanel>
    </Grid>
</Window>
